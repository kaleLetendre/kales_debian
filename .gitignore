# Never commit secrets, even on a private repo.
secrets/

# Per-machine role choice (client|server), written by bootstrap.sh's menu.
.role

# Shipyard plugin scratch (symlink into ~/.claude plugin data). Never tracked.
.shipyard

# Editor backups
*.swp
*.swo
*~
*.bak

# OS junk
.DS_Store
Thumbs.db
